{
  "name": "epubcheck-standalone-cli",
  "version": "5.3.0-build1",
  "description": "The official EPUBCheck command line with no Java to install: same flags, console output, JSON, XML, and XMP reports, and exit codes as the official tool, byte-identical, run straight from npx.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "epubcheck-standalone-cli": "./dist/cli.js"
  },
  "files": [
    "dist/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "//build": "rm -rf dist first so tsc never ships stale outputs of deleted sources (e.g. the removed src/render.ts): unlike the library, this package's whole dist is tsc output with no separately-built engine, so a clean rebuild is safe and complete.",
    "build": "rm -rf dist && tsc -p tsconfig.json && chmod +x dist/cli.js",
    "//prepack": "Local safety net only. The repo-wide .npmrc sets ignore-scripts=true, which disables this hook, so CI/release must build explicitly before publishing (see .github/workflows/release.yml). It fires only for a publish run with lifecycle scripts enabled.",
    "prepack": "npm run build",
    "test": "npm run build && node --test --test-concurrency=2 test/*.test.ts",
    "typecheck:tests": "tsc -p tsconfig.tests.json",
    "test:jar": "npm run build && node test/jar-parity.ts",
    "goldens": "node test/generate-goldens.ts",
    "generate:cli-data": "node scripts/generate-cli-data.ts",
    "generate:locale-messages": "node scripts/generate-locale-messages.ts"
  },
  "dependencies": {
    "epubcheck-standalone": "5.3.0-build1"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  },
  "keywords": [
    "epub",
    "epubcheck",
    "epub3",
    "ebook",
    "validator",
    "validation",
    "cli",
    "command-line",
    "npx",
    "accessibility",
    "opf",
    "ncx",
    "w3c",
    "teavm",
    "standalone"
  ],
  "license": "BSD-3-Clause",
  "author": "John L. Armstrong IV",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jlarmstrongiv/epubcheck-standalone.git",
    "directory": "packages/epubcheck-standalone-cli"
  },
  "homepage": "https://github.com/jlarmstrongiv/epubcheck-standalone/tree/main/packages/epubcheck-standalone-cli#readme",
  "bugs": {
    "url": "https://github.com/jlarmstrongiv/epubcheck-standalone/issues"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
